# Dev 开发栈 v0.7.1 / v0.7.2 升级计划

**日期**: 2026-07-15
**范围**: `dev/` 目录（不动 `version/` 历史快照）
**基线分支**: `cleanup-v0.7.0-alpha`（HEAD `645a6be`，v0.7.0 清理已 Ready-to-merge 但尚未合并）
**新分支**: `restructure-v0.7.1`（从当前 HEAD 切出）

- **v0.7.1** = 文档结构重整（模板单源化、engine/reports/design 归类、README 重写）
- **v0.7.2** = Skill 清理优化（SKILL.md 瘦身 + references/ 拆分、模板引用单源、LGV→LGFV 收尾）

---

## 审查发现（依据）

| # | 发现 | 严重度 |
|---|---|---|
| F1 | 模板双份存放：`dev/design/templates/`（css+type2–15）与 `dev/.claude/skills/.../templates/`（report-template+type13/14/15）**内容已分叉**（skill 副本大 1–3KB） | 🔴 |
| F2 | 缺 Type 1 模板；`report-template.html` 疑似即 Type 1 但未对齐命名 | 🔴 |
| F3 | `dev/engine/` 41 份 md 中 ~15 份为一次性历史审查/自评/审计产物，与 ~26 份现行方法论混放 | 🔴 |
| F4 | `dev/reports/` 71 份报告单层平铺，行业/系统智能/辅助分析/方法论四类混杂 | 🟠 |
| F5 | `design/` 杂物间：模板、样式规格、数据管线、2 份历史 roadmap、2 份历史 audit 混放；`data-pipeline-spec.md` 与 `data/data-architecture.md` 分离 | 🟠 |
| F6 | `dev/industry-validation-report.html` 孤儿文件挂根目录 | 🟡 |
| F7 | README 目录树与磁盘脱节：缺 `design/`、虚构 `archive/`、engine 清单失真；SKILL 版本史 "12-template" vs README "15" | 🟡 |
| F8 | SKILL.md 399 行单文件，无 `references/`；Supporting Files 用仓库绝对路径；持有分叉模板拷贝 | 🟡（0.7.2） |

**已确认决策**：模板单一事实源 = `dev/templates/`（顶层）；reports 按行业分子目录。

**关键技术事实**（移动时必须同步处理）：
- 71 份报告 CSS 链接为 `href="../design/templates/template-base.css"` → 移到 `reports/<sub>/` 后须改为 `../../templates/template-base.css`
- 模板自身以同目录相对路径引用 css（`href="template-base.css"`）→ 模板整体平移不受影响
- 报告间**无** html 互链（已验证）
- `dev/engine/multi-stakeholder.md` 引用 `../design/templates/template-type13/14/15.html` → 改 `../templates/...`
- `dev/engine/paradigm-brand-channel.md` → `../reports/food-beverage-methodology.html`；`paradigm-network-traffic.md` → `../reports/transportation-methodology.html` → 需更新为新子目录路径
- `scripts/consistency_check.py:11` `TEMPLATES_DIR = ROOT/"dev"/"design"/"templates"` → 改 `dev/templates`
- 检查器 `ENGINE_DIR.rglob("*.md")` 递归扫描 → `engine/audits/` 子目录仍在扫描范围内，历史文档继续受检（当前全部通过，移动后仍通过）
- `tests/` 仅引用 `dev/engine` 与 SKILL_FILE，模板/报告移动不破坏测试
- 检查器已内置 `SKILL_REFERENCES_DIR` + `REFERENCE_TO_ENGINE_MAP`（含 `mosaic-engine-architecture.md → mosaic-engine.md`）——references/ 拆分是检查器预留的能力，0.7.2 直接启用

---

# Part A — v0.7.1 文档结构重整

## 目标结构

```
dev/
├── README.md                  ← 重写，与磁盘严格对齐
├── engine/                    ← 现行方法论（26份）
│   └── audits/                ← 历史审查/自评/审计归档（15份）
├── templates/                 ← 模板单一事实源
│   ├── template-base.css
│   ├── template-type1.html    ← 原 report-template.html（需验证其确为 Type 1 规格）
│   └── template-type2..15.html
├── design/
│   ├── report-style-system.md
│   └── archive/               ← v0.6-roadmap、v0.7-roadmap-detailed、phase2/report-audit-findings
├── data/
│   ├── data-architecture.md
│   └── data-pipeline-spec.md  ← 从 design/ 并入
├── product/                   ← 不动
├── reports/
│   ├── solar/  semiconductor/  equipment/  biomedicine/  medicaldevice/  nev/
│   ├── datacenter/  lgv/  textile-apparel/  food-beverage/  transportation/
│   ├── retail/  media-internet/           ← 13 个行业子目录
│   ├── system-intelligence/   ← 传染×4 + 集中度×3 + 系统性警报×1 + engine-validation-stats
│   └── validation/            ← yongmei-retrospective + huachen-multi-stakeholder + industry-validation-report
└── .claude/skills/.../templates/  ← 0.7.1 删除（SKILL.md 同步改指引；深度瘦身归 0.7.2）
```

## 报告归位映射（72 份 = 71 + 根目录孤儿）

| 子目录 | 文件 |
|---|---|
| `solar/` | solar-ja, solar-longi, solar-longi-ext-support, solar-tongwei, pv-bond-dashboard, pv-industry-methodology, pv-longi-vs-yidao-comparison, longi-esg-scan, longi-l22cb-lgd, longi-mosaic-completeness, longi-stress-test（11） |
| `semiconductor/` | semiconductor-hhg, smic-bond-lgd, smic, smic-esg-scan, smic-ext-support, smic-stress-test, ziguang（7） |
| `equipment/` | haas, kede, tuopu（3） |
| `biomedicine/` | beigene, innovent, mabwell（3） |
| `medicaldevice/` | lepu, mindray, wandong（3） |
| `nev/` | byd, byd-esg-scan, leapmotor, xpeng（4） |
| `datacenter/` | chinwey, gds, vnet（3） |
| `lgv/` | lgv-hangzhou, hangzhou-lgd, hangzhou-stress-test, qiancheng, wuhan, hangzhou-ext-support（6） |
| `textile-apparel/` | anta, distressed, hla, methodology（4） |
| `food-beverage/` | distressed, maotai, methodology, yili（4） |
| `transportation/` | cosco, distressed, methodology, sf（4） |
| `retail/` | distressed, jd, methodology, yonghui（4） |
| `media-internet/` | distressed, focusmedia, methodology, tencent（4） |
| `system-intelligence/` | contagion-realestate-2021, contagion-solar-overcapacity, contagion-yongmei-2020, contagion-ziguang-2020, concentration-ideal, concentration-liaoning-heavy, concentration-pv-heavy, systemic-warning-2026q2, engine-validation-stats（9） |
| `validation/` | yongmei-retrospective, huachen-multi-stakeholder, industry-validation-report（3，含根目录孤儿） |

## engine/audits/ 归档清单（15 份）

`capability-review-2026-07-08`、`closure-check-2026-07-08`、`final-review-2026-07-08`、`self-assessment-2026-07-08`、`consistency-audit-v0.5.2`、`v0.6.1-0.6.7-quality-audit`、`v0.7.0-alpha-comprehensive-review`、`remaining-issues-v0.5.0-alpha`、`financial-analysis-audit`、`new-industry-case-audit`、`new-industry-methodology-audit`、`practitioner-usability-audit`、`quantitative-audit`、`rating-agency-benchmark-audit`、`risk-management-standards-audit`

> 历史文档内容**不改动**（含其中的 `dev/reports/...` 旧路径引用）——归档即冻结。

---

## Task 1: 建分支 + 模板单源化

**Files:**
- `git checkout -b restructure-v0.7.1`
- Move: `dev/design/templates/*` → `dev/templates/`
- Rename: `dev/.claude/skills/.../templates/report-template.html` → `dev/templates/template-type1.html`（先验证内容确为基础信用报告模板/Type 1 规格：`grep -i "type 1\|基础信用" report-template.html dev/design/report-audit-findings.md`）
- Delete: `dev/.claude/skills/.../templates/`（type13/14/15 副本删除前逐文件 diff 确认无独有内容：`diff dev/design/templates/template-typeN.html <skill副本>`；若有独有修改，先并入 dev/templates/ 版本再删）

- [ ] **Step 1**: diff 三对分叉模板，确认 design 版为权威（v0.7.0 清理后的版本），skill 副本无独有修改
- [ ] **Step 2**: `mkdir dev/templates && git mv dev/design/templates/* dev/templates/`，`git mv` skill 的 report-template.html 为 `dev/templates/template-type1.html`，删除 skill templates/ 目录
- [ ] **Step 3**: 更新 `scripts/consistency_check.py:11` `TEMPLATES_DIR` → `ROOT/"dev"/"templates"`
- [ ] **Step 4**: 更新 `dev/engine/multi-stakeholder.md` 中 3 处 `../design/templates/` → `../templates/`
- [ ] **Step 5**: 验证 `python scripts/consistency_check.py` PASSED；`python -m pytest tests/ -q` 全绿；抽查 2 份模板渲染（css 同目录引用不受移动影响）
- [ ] **Step 6**: Commit `refactor(templates): consolidate templates into dev/templates as single source of truth`

## Task 2: engine 历史产物归档

- [ ] **Step 1**: `mkdir dev/engine/audits && git mv` 上表 15 份文件入 `audits/`
- [ ] **Step 2**: 检查现行方法论文档是否交叉引用这 15 份（`grep -rn "audit\|review-2026\|closure-check\|self-assessment\|remaining-issues" dev/engine/*.md`，排除 audits/ 自身）；若有现行文档引用，更新链接为 `audits/xxx.md`
- [ ] **Step 3**: 验证 checker PASSED（audits/ 仍在 rglob 范围内）+ pytest 全绿
- [ ] **Step 4**: Commit `refactor(engine): archive historical audits and reviews to engine/audits/`

## Task 3: reports 按行业归子目录 + CSS 链接批量重写

- [ ] **Step 1**: 按映射表 `git mv` 72 份报告入 16 个子目录（13 行业 + system-intelligence + validation）
- [ ] **Step 2**: 批量重写 CSS 链接：在所有移动后的报告中 `../design/templates/template-base.css` → `../../templates/template-base.css`（`sed -i` 或脚本；改前统计 71 处，改后 `grep -rn "design/templates" dev/reports/ | wc -l` = 0）
- [ ] **Step 3**: 更新 engine 现行文档中的报告链接：`paradigm-brand-channel.md` → `../reports/food-beverage/food-beverage-methodology.html`；`paradigm-network-traffic.md` → `../reports/transportation/transportation-methodology.html`；全库 grep `](../reports/` 确认无遗漏
- [ ] **Step 4**: 抽查渲染：system-intelligence、solar、lgv 各 1 份在浏览器打开确认 css 生效（或 grep 验证 link 行）
- [ ] **Step 5**: 验证 checker + pytest
- [ ] **Step 6**: Commit `refactor(reports): organize 72 reports into industry and system-intelligence subdirectories`

## Task 4: design/ 与 data/ 归位

- [ ] **Step 1**: `git mv dev/design/data-pipeline-spec.md dev/data/`
- [ ] **Step 2**: `mkdir dev/design/archive && git mv` v0.6-roadmap.md、v0.7-roadmap-detailed.md、phase2-audit-findings.md、report-audit-findings.md 入 archive/
- [ ] **Step 3**: grep 检查现行文档对这四个文件的引用并更新路径
- [ ] **Step 4**: 验证 checker + pytest
- [ ] **Step 5**: Commit `refactor(design,data): move pipeline spec to data/, archive historical roadmaps and audits`

## Task 5: README 重写 + SKILL.md 模板引用修正

**README 重写要点**（与磁盘逐一核对）：
- 目录树：真实反映 engine/（+audits/）、templates/、design/（+archive/）、data/（2份）、product/、reports/（16子目录+份数）、.claude/skills
- 删除虚构 `archive/` 行；报告导航表按新子目录更新
- 版本历史补 v0.7.1 行；版本号 `v0.7.0-alpha` → `v0.7.1-alpha`；更新日期 2026-07-15

**SKILL.md 最小修正**（深度瘦身归 0.7.2）：
- Supporting Files 中 `templates/report-template.html`、`templates/template-type13/14/15.html` 四条 → 指向 `dev/templates/template-typeN.html`
- 版本史 "12-template" → 15

- [ ] **Step 1–4**: 重写 README、修 SKILL.md 引用、验证（checker 含 SKILL 检查）、commit `docs(readme,skill): rewrite README to match restructured layout; point skill at single-source templates`

## Task 6: 防漂移回归 + v0.7.1 快照

- [ ] **Step 1**: `consistency_check.py` 增加检查：若 `dev/.claude/skills/.../templates/` 存在则报错（`SKILL_TEMPLATE_DRIFT`），防止模板双份复发
- [ ] **Step 2**: 全量验证：`pytest tests/ -q`、`consistency_check.py`、TOC 抽查
- [ ] **Step 3**: 刷新快照 `version/v0.7.1-alpha/` + `.zip`（沿用 v0.7.0 流程：排除 .git、settings.local.json，Compress-Archive，unzip -t 验证）
- [ ] **Step 4**: Commit `release: v0.7.1-alpha dev-stack structure reorganization`

---

# Part B — v0.7.2 Skill 清理优化

## Task 7: SKILL.md 瘦身 + references/ 拆分

- [ ] **Step 1**: 从 SKILL.md 抽取详情段落为 `references/*.md`，文件名对齐 engine 对应物（复用检查器 `REFERENCE_TO_ENGINE_MAP` 机制，版本头一致）：
  - `references/mosaic-engine-architecture.md` ← Mosaic Engine Architecture（Mode A、置信度、密度、Gap映射、完备性输出）
  - `references/system-intelligence.md` ← System-Intelligence Layer（传染、五维集中度、SRI）→ 对照 `systemic-warning-framework.md`
  - `references/industry-scoring.md` ← Track A 金字塔 + D1–D10 + C1–C4 → 对照 `industry-framework.md`
  - `references/validation-cases.md` ← Black-Swan Retrospective + Validated Industries & Cases 表
- [ ] **Step 2**: SKILL.md 主体压缩为导航+强制规则（Invocation Protocol、When to Use、Mandatory Density Rules、Mode B 护栏、双轨核心、Key Design Principles、references 索引、版本史），目标 ≤200 行
- [ ] **Step 3**: Supporting Files 清单修正（全部改为有效相对引用）
- [ ] **Step 4**: 验证 checker（SKILL_REF 版本头比对生效）+ pytest
- [ ] **Step 5**: Commit `refactor(skill): slim SKILL.md into navigator + references/ split`

## Task 8: LGV→LGFV 命名收尾

- [ ] **Step 1**: `git mv dev/engine/lgv-framework.md dev/engine/lgfv-framework.md`，标题及正文 LGV→LGFV（保留首次出现处注明"原 LGV"）
- [ ] **Step 2**: 全库扫描 `LGV`/`lgv-`（engine、skill、README、reports 文件名 lgv-* 是否一并改名——执行时与用户确认范围）
- [ ] **Step 3**: 更新 checker 中 ENGINE_DOCS 清单、README、SKILL.md 引用
- [ ] **Step 4**: 验证 + commit `refactor(naming): unify LGV to LGFV across dev stack`

## Task 9: v0.7.2 快照 + 收尾

- [ ] **Step 1**: 全量验证三件套（pytest / checker / TOC）
- [ ] **Step 2**: 刷新 `version/v0.7.2-alpha/` + `.zip`
- [ ] **Step 3**: README 版本史补 v0.7.2 行
- [ ] **Step 4**: Commit `release: v0.7.2-alpha skill cleanup and naming unification`

---

## 执行方式

沿用 subagent-driven：每 Task 派发 implementer → review-package → reviewer → 修复循环，ledger 记于 `.superpowers/sdd/progress.md`。Task 1–6（0.7.1）连续执行后进入 Task 7–9（0.7.2）。

**风险与注意**：
- 大量 `git mv` 必须在同一 Task 内与链接重写**原子完成**，避免中间态提交破坏渲染
- 历史归档文档内容冻结，不为其更新旧路径引用
- `version/0.4.0/` 删除等 v0.7.0 遗留工作树噪音不在本计划范围（随 v0.7.0 分支合并一并处理）
